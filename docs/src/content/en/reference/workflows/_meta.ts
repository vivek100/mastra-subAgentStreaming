const meta = {
  workflow: "Workflow",
  step: "Step",
  then: ".then()",
  branch: ".branch()",
  parallel: ".parallel()",
  commit: ".commit()",
  "create-run": ".createRunAsync()",
  start: ".start()",
  resume: ".resume()",
  watch: ".watch()",
  stream: ".stream()",
  streamVNext: ".streamVNext()",
  dowhile: ".dowhile()",
  dountil: ".dountil()",
  foreach: ".foreach()",
  map: ".map()",
  sleep: ".sleep()",
  sleepUntil: ".sleepUntil()",
  waitForEvent: ".waitForEvent()",
  sendEvent: ".sendEvent()",
  execute: ".execute()",
  snapshots: "Snapshots",
};

export default meta;
